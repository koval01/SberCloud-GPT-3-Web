function shuffle(array) {
  var currentIndex = array.length,  randomIndex;
  while (0 !== currentIndex) {
    randomIndex = Math.floor(Math.random() * currentIndex);
    currentIndex--;
    [array[currentIndex], array[randomIndex]] = [
      array[randomIndex], array[currentIndex]];
  }
  return array;
}
function format_result(start_, list_) {
  let result_list = [];
  list_ = list_.replies
  
  for (let i = 0; i < list_.length; i++) {
    result_list.append("<figure><blockquote class=\"blockquote\"><b>"+start_+"</b><i>"+list_[i]+"</i></blockquote><figcaption class=\"blockquote-footer\">story.q-writer.com</figcaption></figure>");
  }
  return result_list;
}
function generate_continue() {
    var urls = [
        'https://orange-shadow.koval.workers.dev/',
    ];
    const len_c = 60;
    const button = $("#generate_continue_button"),
        loader = button.find("span");
    const txt_c = $("textarea").val();
    shuffle(urls);
    
    for (let i = 0; i < urls.length; i++) {
        $.ajax({
          url: urls[i],
          type: "POST",
          contentType: "application/json",
          data: JSON.stringify({
            prompt: txt_c,
            length: len_c,
          }),
          beforeSend: function () {
          },
          success: function (o) {
            let block = $("#result_continue_block");
            alert(o);
            block.empty(), block.append(format_result(txt_c, o));
          },
          error: function () {
            alert("Извините, но произошла ошибка...");
          },
        });
    }
}
